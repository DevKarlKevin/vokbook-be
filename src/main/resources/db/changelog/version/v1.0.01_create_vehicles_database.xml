<databaseChangeLog
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd">

    <changeSet id="2022-01-09_create_vehicles_table" author="Siim Salla">
        <sql>
            create table vehicle
            (
                id bigserial not null,
                comodule_id varchar(50),
                identifier varchar(50),
                fleet varchar(255) not null,
                model varchar(255) not null,
                mileage bigserial,
                status varchar(255) not null,
                repo varchar(255) not null,
                ecu_branch varchar(255) not null,
                ecu_commit varchar(255) not null,
                cs_commit varchar(255) not null,
                created_at timestamp default now() not null,
                updated_at timestamp default now() not null,
                deleted_at timestamp
            );

            insert into vehicle (comodule_id, identifier, fleet, model, mileage, status, repo, ecu_branch, ecu_commit, cs_commit)
            values ('TestSõiduk', 'ID122', 'Tallinn', 'VOK1', 0, 'Active', '123cefg', '123cefg', '123cefg', '123cefg')
        </sql>
    </changeSet>
</databaseChangeLog>
